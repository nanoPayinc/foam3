p({
  "class": "foam.core.boot.CSpec",
  "name": "boxThreadPool",
  "lazy": false,
  "serve": false,
  "serviceScript": `
    return new foam.core.pool.ThreadPoolAgency.Builder(x)
      .setPrefix("box")
      .build();
  `
})
